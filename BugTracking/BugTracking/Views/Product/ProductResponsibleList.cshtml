@model IEnumerable<BugTracking.Models.Responsible>

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-12">
        <h2>Responsible Person</h2>
    </div>
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="ibox float-e-margins">

            <div class="ibox-content">

                <a class="btn btn-primary" href=@Url.Action("AssignResponsible", "Product") role="button">New</a>

                <table class="table table-hover">
                    <thead>
                        <tr>

                            <th>@Html.DisplayNameFor(model => model.Product.Name_Prod)</th>
                            <th>@Html.DisplayNameFor(model => model.Product.Description_Prod)</th>
                            <th>@Html.DisplayNameFor(model => model.User.FullName)</th>
                            <th>@Html.DisplayNameFor(model => model.User.TelephoneNumber)</th>
                            <th>@Html.DisplayNameFor(model => model.User.Email)</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>


                                <td>@Html.DisplayFor(modelItem => item.Product.Name_Prod)</td>
                                <td>@Html.DisplayFor(modelItem => item.Product.Description_Prod)</td>
                                <td>@Html.DisplayFor(modelItem => item.User.FullName)</td>
                                <td>@Html.DisplayFor(modelItem => item.User.TelephoneNumber)</td>
                                <td>@Html.DisplayFor(modelItem => item.User.Email)</td>
                                <td class="text-navy">
                                    <a href=@Url.Action("EditResponsible", "Product", new {id=item.ID})><i class="fa fa-cog"></i></a>
                                    <a href=@Url.Action("DeleteResponsible", "Product", new {id=item.ID})><i class="fa fa-trash"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>